// Define potential API error structure (adjust as needed)
export interface ApiError {
    message: string | string[];
    error?: string;
    statusCode?: number;
}

// Add other shared API-related types here if needed
